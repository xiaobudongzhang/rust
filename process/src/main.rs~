use std::error::Error;
use std::io::prelude::*;
use std::process::{Command, Stdio};


statci PANGRAM: &'static str =
"the quik brown fox jumped over the lazy dog";
fn main() {
   let process = match Command::new("wc")    

}
